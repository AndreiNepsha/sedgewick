#include <utility>
#include <vector>

typedef std::vector<std::pair<int, int>> alg1IOType;

alg1IOType* fast_search(alg1IOType* in);
alg1IOType* fast_merge(alg1IOType* in);
alg1IOType* weighed_fast_merge(alg1IOType* in);
alg1IOType* weighed_fast_merge_with_path_compresion(alg1IOType* in);